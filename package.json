{
  "name": "internpulse",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.worker.json && tsc -p tsconfig.json && vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler deploy",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply internpulse --local",
    "db:migrate:remote": "wrangler d1 migrations apply internpulse --remote",
    "db:seed:local": "wrangler d1 execute internpulse --local --file=./seed/dev-seed.sql",
    "db:seed:remote": "wrangler d1 execute internpulse --remote --file=./seed/dev-seed.sql",
    "demo:seed": "node scripts/demo-seed.mjs",
    "test": "vitest run",
    "load-test": "node scripts/load-test.mjs"
  },
  "dependencies": {
    "agents": "^0.22.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.0.0",
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@types/node": "^26.4.1",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.129.1"
  }
}
